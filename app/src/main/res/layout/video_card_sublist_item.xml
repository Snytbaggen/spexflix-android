<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/video_extra_card_height"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:padding="8dp"
    android:background="@color/lightGrey">

    <ImageView
        android:id="@+id/video_card_sub_item_play"
        android:layout_width="32dp"
        android:layout_height="32dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginEnd="8dp"
        android:src="@drawable/play_large"/>

    <TextView
        android:id="@+id/video_card_sub_item_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toStartOf="@id/video_card_sub_item_play"
        android:layout_marginEnd="16dp"
        android:textColor="@color/black"
        android:textSize="18sp"
        android:fontFamily="sans-serif-medium"
        tools:text="Föreställning 14 feb"/>

    <TextView
        android:id="@+id/video_card_sub_item_video_type"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/video_card_sub_item_name"
        app:layout_constraintEnd_toStartOf="@id/video_card_sub_item_play"
        android:layout_marginEnd="16dp"
        android:textColor="@color/subtitleTextColor"
        android:textSize="14sp"
        tools:text="Föreställning"/>

    <TextView
        android:id="@+id/video_card_sub_item_description"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/video_card_sub_item_play"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="2dp"
        android:lines="2"
        android:ellipsize="end"
        android:gravity="center_vertical"
        android:textColor="@color/black"
        android:textSize="14sp"
        tools:text="@string/lipsum"/>

</androidx.constraintlayout.widget.ConstraintLayout>